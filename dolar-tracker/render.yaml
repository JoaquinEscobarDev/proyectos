services:
  - type: web
    name: dolar-tracker
    env: node
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: EXCHANGE_API_KEY
        sync: false
      - key: FRONTEND_URL
        sync: false
